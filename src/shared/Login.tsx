import React, { Fragment } from 'react';
import {Login as LoginForm} from '../features/auth/components/Login';

export const Login = () => {
  return (
    <Fragment>
      <LoginForm/>
    </Fragment>
  )
}